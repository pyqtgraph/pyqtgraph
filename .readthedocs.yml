# Read the Docs configuration file
# https://docs.readthedocs.io/en/stable/config-file/v2.html

version: 2

python:
  version: 3.8
  install:
    - requirements: doc/requirements.txt

sphinx:
  # until we sort out gallery warnings
  fail_on_warning: false

build:
  apt_packages:
    - xvfb

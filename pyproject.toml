[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"


[tool.black]
line-length = 88
target-version = ['py38']
include = '\.pyi?$'
exclude = '''

(
  /(
      \.eggs         # exclude a few common directories in the
    | \.git          # root of the project
    | \.hg
    | \.mypy_cache
    | \.tox
    | \.venv*
    | _build
    | buck-out
    | build
    | dist
  )/

)
'''

[tool.isort]
profile = "black"
honor_noqa = true
color_output = true
py_version = 38
src_paths = ["pyqtgraph", "tests"]
skip_glob = ["**/*Template*.py", "**/colorama"]
skip_gitignore = true
known_third_party = ["QtCore", "QtGui", "QtWidgets"]

[tool.pycln]
all = true
exclude = '(Template|__init__.py)'

[tool.mypy]
packages = ["pyqtgraph"]
show_error_codes = true
strict = true
disable_error_code = ["attr-defined", "union-attr", "misc", "unused-ignore"]
ignore_errors = true
ignore_missing_imports = true
disallow_untyped_calls = false
disallow_any_generics = false
disallow_subclassing_any = false
warn_return_any = false
enable_incomplete_feature = ["Unpack"]

[[tool.mypy.overrides]]
module = "pyqtgraph.flowchart.*"    # the list will increase
ignore_errors = false   # will be changed to `false` when it's ready

[[tool.mypy.overrides]]
module = "pyqtgraph.examples.Flowchart"    # the list will increase
ignore_errors = false   # will be changed to `false` when it's ready

[[tool.mypy.overrides]]
module = "pyqtgraph.examples.FlowchartCustomNode"    # the list will increase
ignore_errors = false   # will be changed to `false` when it's ready